import React from 'react';

function Description(props) {
    return ( <
        div className = "multi-switch_multi_switch_description__1AMHw" > {
            props.description
        } < /div>
    );
}

export default Description;